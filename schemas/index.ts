const product = {
    name: 'product',
    type: 'document',
    // ... rest of the schema definition
}

const category = {
    name: 'category',
    type: 'document',
    // ... rest of the schema definition
}

export default product;

export const schemaTypes = [product, category];